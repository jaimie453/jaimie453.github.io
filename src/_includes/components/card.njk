{% macro createCard(title, description, url, image, tags, useH2 = false) %}
  <li class="card">
    <img class="card__image" src="../../assets/images/{{ image }}" alt=""/>
    <a
      href="{{ url }}" class="card__link">
      {# for use on project/articles pages #}
      {% if useH2 %}
        <h2 class="card__title font-size-xl">{{ title }}</h2>
      {% else %}
        <h3 class="card__title">{{ title }}</h3>
      {% endif %}
    </a>
    <p class="card__body">
      {{ description }}
    </p>
    <p class="visually-hidden">Tagged:</p>
    <ul class="card__tags">
      {% for tag in tags %}
        <li>{{ tag }}</li>
      {% endfor %}
    </ul>
  </li>
{% endmacro %}